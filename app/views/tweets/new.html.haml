%h2 Create New Tweet

=	form_tag tweets_path, :method => :post do

	=	label :tweet, :userid, 'User Id'
	=	text_field :tweet, :userid

	=	label :tweet, :text, 'Tweet Body'
	=	text_field :tweet, :text

	=	submit_tag 'Save Changes'